const fs = require('fs')

async function codegen(nodeType, filepath, docsPath = null) {
    try {
        const data = await nodeType.generateHTML(docsPath)
        fs.writeFileSync(filepath, data)
    } catch (e) {
        console.error('Error generating HTML template', e)
    }
}

module.exports = codegen