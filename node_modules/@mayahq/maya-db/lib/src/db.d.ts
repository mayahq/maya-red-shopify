import { Collection } from "./storage/collection";
export declare function localDb({ encryptionKey, root }: {
    encryptionKey: string;
    root: string;
}): Collection;
export declare function mongoDbAdapter({ root }: {
    root: string;
}): Collection;
export declare function httpAdapter({ apiUrl, root, headers }: {
    apiUrl: string;
    root: string;
    headers: any;
}): Collection;
