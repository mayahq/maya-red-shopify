declare type LockAcquireOpts = {
    acquireFor: number;
    pollInterval: number;
    timeout: number;
};
export declare class MongoLock {
    generateId(): string;
    acquire(path: string, callback: Function, opts?: LockAcquireOpts): Promise<any>;
    release(blockPath: string, lockId: string): Promise<void>;
}
export default MongoLock;
