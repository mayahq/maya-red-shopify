"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateSecretKey = exports.decrypt = exports.encrypt = void 0;
var crypto_1 = __importDefault(require("crypto"));
var ALGORITHM = 'aes-256-cbc';
function encrypt(text, key) {
    var bufkey = Buffer.from(key, 'hex');
    var iv = crypto_1.default.randomBytes(16);
    var cipher = crypto_1.default.createCipheriv(ALGORITHM, bufkey, iv);
    var encrypted = cipher.update(text);
    encrypted = Buffer.concat([encrypted, cipher.final()]);
    return {
        iv: iv.toString('hex'),
        encryptedData: encrypted.toString('hex')
    };
}
exports.encrypt = encrypt;
function decrypt(encryptedText, key, iv) {
    var bufkey = Buffer.from(key, 'hex');
    var encryptedData = Buffer.from(encryptedText, 'hex');
    var decipher = crypto_1.default.createDecipheriv(ALGORITHM, bufkey, Buffer.from(iv, 'hex'));
    var decrypted = decipher.update(encryptedData);
    decrypted = Buffer.concat([decrypted, decipher.final()]);
    return decrypted.toString();
}
exports.decrypt = decrypt;
function generateSecretKey() {
    var newKey = crypto_1.default.randomBytes(32).toString('hex');
    return newKey;
}
exports.generateSecretKey = generateSecretKey;
